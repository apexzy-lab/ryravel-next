import JournalVolume from "../JournalVolume";
import "../journal.css";

export const metadata = {
  title: "Ryravel Journal, Vol. 1: Stillness",
  description: "Four accounts of what happens when the motion stops and nothing rushes in to replace it.",
};

export default function VolumeOnePage() {
  return <JournalVolume />;
}
