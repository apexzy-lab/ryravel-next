import { buildMetadata } from "../seo";

export const metadata = buildMetadata({ title: "Cookie Notice & Choices", description: "Ryravel's essential site functions, optional marketing tracking and how to change your cookie choice.", path: "/cookies" });

export default function CookiesLayout({ children }) { return children; }
